services:
  mongo:
    image: mongo:7
    container_name: manga-reader-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - manga_reader_mongo:/data/db

volumes:
  manga_reader_mongo:
